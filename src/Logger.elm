module Logger exposing (logError)


logError : String -> Cmd msg
logError _ =
    -- This needs to be implemented, but it's here so that I don't need to go back and put it in to all the places I want to call it later
    Cmd.none
